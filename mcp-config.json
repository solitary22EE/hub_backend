{
  "mcpServers": {
    "postgres": {
      "type": "command",
      "command": "npx",
      "args": ["-y", "@itunified.io/mcp-postgres"],
      "env": {
        "POSTGRES_CONNECTION_STRING": "${DATABASE_URL}"
      }
    }
  }
}
